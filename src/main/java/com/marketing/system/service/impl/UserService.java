package com.marketing.system.service.impl;

import com.marketing.system.entity.UserInfo;

public class UserService {
/*    public UserInfo loginAuto(String id, String password) {
        UserInfo user=new UserInfo();
        user = userDao.findUserByUserId(Integer.parseInt(id));
        if(user!=null) {
            if(password.equals(user.getPassword().trim().toUpperCase())) {
                return user;
            }else {
                throw new UsersNotFoundException("登陆失败");
            }
        }else {
            throw new UsersNotFoundException("登陆失败");
        }



    }*/
}
